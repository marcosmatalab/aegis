// Placeholder home page. The real scorecards/trends land in later commits; this
// scaffold commit only proves the Next.js + TS + Recharts toolchain builds offline.
export default function Page() {
  return (
    <main style={{ padding: "2rem", maxWidth: 960, margin: "0 auto" }}>
      <h1>Aegis dashboard</h1>
      <p style={{ color: "#9aa0aa" }}>
        Read-only view of Aegis&apos;s real reports — assembled commit by commit.
      </p>
    </main>
  );
}

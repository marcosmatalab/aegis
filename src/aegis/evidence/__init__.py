"""F8 governance evidence: map Aegis's real artifacts to framework controls.

This package only AGGREGATES + MAPS the artifacts F1.x–F7 already produce (eval /
red-team / calibration JSON reports + the effective guardrail config); it generates
no new measurements. Every control's status is DERIVED from a real artifact field,
never asserted — see :mod:`aegis.evidence.builder`.
"""
